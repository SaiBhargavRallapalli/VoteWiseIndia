{
  "name": "votewise-india",
  "version": "2.0.0",
  "description": "AI-powered Indian election education assistant using 9 Google Cloud services: Gemini AI, Cloud Translation, Text-to-Speech, Vision API, Natural Language, BigQuery, Firebase Auth, Firestore, and Analytics",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint server.js middleware/ routes/ services/ data/",
    "test": "NODE_ENV=test jest --coverage --forceExit --runInBand",
    "test:e2e": "npx playwright test",
    "test:all": "npm run lint && npm test && npm run test:e2e"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "firebase-admin": "^12.2.0",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000,
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/e2e/"
    ]
  }
}
